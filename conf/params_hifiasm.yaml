# conf/params_hifiasm.yaml
parameters:
  # Individual parameter flags
  post_join:
    - name: "with-post-join"
      flag: ""                    # Default is to include post-join
    - name: "no-post-join"
      flag: "--no-post-join"
  
  quality_threshold:
    - name: "default-quality"
      flag: ""                    # Default quality threshold
    - name: "lowQ50"
      flag: "--lowQ 50"

  # Add more parameter categories
